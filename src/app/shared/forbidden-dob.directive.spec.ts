import { ForbiddenDobDirective } from './forbidden-dob.directive';

describe('ForbiddenDobDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenDobDirective();
    expect(directive).toBeTruthy();
  });
});
