import { InvalidSinDirective } from './invalid-sin.directive';

describe('InvalidSinDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidSinDirective();
    expect(directive).toBeTruthy();
  });
});
